=form_tag({:action=>:create}, options) do
  .fields=render :partial=>partial
  .actions
    =submit_tag("labels.create".t, :disable_with=>"labels.please_wait".t)
    =link_to("labels.cancel".t, :back)
