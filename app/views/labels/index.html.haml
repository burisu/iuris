=toolbar do
  =tool("Nouveau mot-clé", new_label_url, :icon => :new)

-paginator = will_paginate(@labels)
=paginator
.items=render :partial =>  @labels, :spacer_template => "shared/spacer"
=paginator
