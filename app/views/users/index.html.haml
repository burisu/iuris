-if current_user.administrator?
  .toolbar
    =link_to("Nouvel utilisateur", new_user_url)

-paginator = will_paginate(@users)
=paginator
.items
  =render :partial => @users, :spacer_template => "shared/spacer"
=paginator
