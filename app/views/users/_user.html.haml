.item{ :class => (user.activated? ? nil : 'item-disabled' )}
  = logo_tag(avatar_url(user, :size => 48))
  %h3.title= link_to(user.full_name, user_url(user))
  .meta-info
    %span Enregistré
    %span.ago= on(user.created_at)
  .end
